#!/bin/sh
# wily-guile --- drop in replacement for "@GUILE@" that knows a few tricks

top_builddir=@abs_top_builddir@
export top_builddir

top_srcdir=@top_srcdir@
export top_srcdir

GUILE_LOAD_PATH=$top_builddir
export GUILE_LOAD_PATH

exec @GUILE@ "$@"

# wily-guile ends here
